
language: bash

install: ./setup

script:
  - export PATH="$HOME/miniconda/bin:$PATH" 
  - ./build
  - zip -r slides.zip slides.html reveal.js-3.5.0 images
  - curl --upload-file slides.zip https://transfer.sh/slides.zip
